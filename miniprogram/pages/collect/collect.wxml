<scroll-view>
  <view class="container">
    <view class="article">
      <view class="item" bindtap="articleDetail" wx:for="{{collectList}}" data-index="{{index}}" wx:key>
        <article-cmp articleData="{{item}}" />
      </view>
    </view>
  </view>
</scroll-view>